name: Run Script

on: push

jobs:
  apply:
    name: Apply
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run myscript Script
        run: sh ./myscript.sh
        shell: bash
